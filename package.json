{
  "name": "grpc-lambda-poc",
  "version": "0.0.1",
  "description": "A Go gRPC in Lambda POC ",
  "scripts": {
    "start": "sls offline --P 3001 -e SLS_DEBUG=*",
    "local-invoke": "sls invoke local -f gateway -e SLS_DEBUG=* --docker-arg='--network docker_default'"
  },
  "author": "Andrew Marine",
  "license": "UNLICENSED",
  "devDependencies": {
    "serverless-offline": "^5.12.1"
  }
}
